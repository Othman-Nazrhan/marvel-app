import React from 'react'

const Quiz = (props) => {

    console.log(props.userData)
    return (
        <div>
            
            {/* Nom:{name} */}
        </div>
    )
}

export default Quiz ;